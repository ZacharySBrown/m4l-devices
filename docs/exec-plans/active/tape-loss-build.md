# Tape Loss MKII — Build Exec Plan

> Generated by `forge_device.analyzer`. Spec hash: `5f6a62295b7e2f8627c2bc6a8b5410d0178a9beee73e0e0a37585d8394aadeb9`

## Manual Steps Required

These are the steps that genuinely need a human. The harness MUST gate the
build on `--confirm-tested` for each.

- **Drop tape-loss.amxd onto an audio track in Ableton** (~10s) — LiveAPI cannot programmatically add a M4L device to a chain
- **Open standalone debug.maxpat in Max IDE; verify zero console errors** (~10s) — Max IDE required to verify subpatch wiring before Ableton load
- **Play test audio through device; confirm no crashes / clicks** (~30s) — Audio quality requires human ear (oracle) for tuning
- **Calibrate WOW depth (~±15 cents at noon, ~±70 cents at max)** (~60s) — Subjective tuning — depth feel
- **(Phase 5 only) IR capture session — REW + reamp + sine sweep** (~7200s) — Hardware-in-the-loop capture; cannot be automated

**Total estimated human time per build cycle:**
7310 seconds

## Phase 1 — DSP design (pedal-dsp owns)

For each module in the signal chain that has `type: dsp`:

1. Write `docs/design-docs/dsp/<module>-design.md` (block diagram, transfer function, citations to RBJ/JOS/Smith)
2. Write `dsp/reference/<module>.py` (numpy reference implementation)
3. Verify reference produces sensible output (plot or audio render)
4. Hand to engineer with: design doc + reference + audio fixture

## Phase 2 — Module patch generation (pedal-engineer owns)

- [ ] **1. Module `tl_saturate`** — DSP design (pedal-dsp) → patch generation (pedal-engineer) → verify → audit `module.tl_saturate.complete`
- [ ] **2. Module `tl_model_eq`** — DSP design (pedal-dsp) → patch generation (pedal-engineer) → verify → audit `module.tl_model_eq.complete`
- [ ] **3. Module `tl_failure`** — DSP design (pedal-dsp) → patch generation (pedal-engineer) → verify → audit `module.tl_failure.complete`
- [ ] **4. Module `tl_wow`** — DSP design (pedal-dsp) → patch generation (pedal-engineer) → verify → audit `module.tl_wow.complete`
- [ ] **5. Module `tl_flutter`** — DSP design (pedal-dsp) → patch generation (pedal-engineer) → verify → audit `module.tl_flutter.complete`
- [ ] **6. Module `tl_aux`** — DSP design (pedal-dsp) → patch generation (pedal-engineer) → verify → audit `module.tl_aux.complete`
- [ ] **7. Module `tl_volume_mix`** — DSP design (pedal-dsp) → patch generation (pedal-engineer) → verify → audit `module.tl_volume_mix.complete`
- [ ] **8. Module `tl_dry_mix`** — DSP design (pedal-dsp) → patch generation (pedal-engineer) → verify → audit `module.tl_dry_mix.complete`
- [ ] **9. Module `tl_noise`** — DSP design (pedal-dsp) → patch generation (pedal-engineer) → verify → audit `module.tl_noise.complete`

## Phase 3 — Main patch assembly

- [ ] Wire all module subpatches in signal-chain order via `stemforge_bridge.patcher`
- [ ] Add `[plugin~ N]` and `[plugout~ N]` (pitfall #7)
- [ ] Add presentation-mode UI: knobs, switches, dip-toggles per parameter table
- [ ] Add `[live.comment]` for any dynamic text (pitfall #3)
- [ ] Pack `.amxd` via `amxd_pack.pack_amxd(..., device_class='audio')`
- [ ] Run all `PATCHER_VERIFIERS` + `AMXD_VERIFIERS` — must all pass

## Phase 4 — Standalone debug harness (pitfall #9)

- [ ] Generate `device/amxd/<name>-debug.maxpat` — load + monitor patch
- [ ] **Manual:** Open in Max, click loadbang, verify zero console errors (~10s)

## Phase 5 — Live integration test

- [ ] **Manual:** Drop `.amxd` on an audio track in Ableton Live (~10s)
- [ ] **Manual:** Play test audio through device, confirm no crashes (~20s)
- [ ] If telemetry wired: run `forge-telemetry render <input>` for golden fixture

## Phase 6 — Distribution

- [ ] Build `.pkg` if `distribution.format == pkg`
- [ ] Test install + uninstall on clean macOS user
- [ ] Tag release; archive audit trail to `docs/exec-plans/completed/`

## Audit-trail expectations

The `forge-device build` run MUST emit at minimum these events:

- `forge.start` (with `spec_hash`)
- `spec.parsed` (with module + param counts)
- `module.<name>.start` / `.complete` for every module in signal_chain
- `module.<name>.patch_generated` (with sha256)
- `verifier.<rule>` for every verifier (pass + fail)
- `module.<name>.packed` (with sha256 + bytes)
- `device.assembled` (main `.amxd` sha256 + bytes)
- `manual_step_required` for every step in `manual_steps`
- `forge.complete` (with duration_ms)
